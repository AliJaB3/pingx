# گزارش فروش و نرخ تبدیل

## متریک‌ها
- **Revenue**: جمع مبالغ `purchases.price` در بازه.
- **Orders**: تعداد خریدها.
- **Unique buyers**: تعداد خریداران یونیک.
- **AOV**: متوسط ارزش سفارش = Revenue / Orders.
- **Conversion (ساده)**: خریداران یونیک / کل کاربران ثبت‌شده.
- **Funnel Conversion**: `purchase_success` / `checkout_initiated` (رویدادهای ثبت شده).
- رویدادها: `checkout_initiated`, `purchase_success` از بات و `start`, `view_plans` در جدول `events` ذخیره می‌شوند.

## بازه‌ها
- دکمه‌های آماده: امروز، ۷ روز اخیر، ۳۰ روز اخیر (از منوی «📈 گزارش‌ها» در ادمین).

## نکات
- قیمت ذخیره‌شده در خرید، مبلغ نهایی پس از تخفیف است.
- شمارش کاربران کل از جدول `users` گرفته می‌شود.
- اگر رویدادی ثبت نشده باشد، نرخ funnel صفر نشان داده می‌شود.
