# امکانات ادمین

## تخفیف سراسری
- مسیر: ادمین → «🎁 تخفیف سراسری»
- بازه معتبر: ۰ تا ۹۰ درصد
- اثر: نمایش قیمت در لیست پلن‌ها + مبلغ کسر از کیف پول و ذخیره در خرید. قیمت اصلی نیز برای شفافیت نمایش داده می‌شود.

## مرتب‌سازی پلن‌ها
- مسیر: ادمین → پلن → «⬆️ بالا / ⬇️ پایین»
- ترتیب نمایش کاربر بر اساس `sort_order` سپس شناسه پلن است.
- مقدار `sort_order` در دیتابیس ذخیره می‌شود؛ دکمه‌ها با پلن همسایه جابه‌جا می‌کنند.

## تیم و نقش‌ها
- ادمین‌ها: با `ADMIN_IDS` یا از طریق settings اضافه می‌شوند.
- پشتیبان‌ها: از ادمین → «🎧 مدیریت پشتیبان‌ها» افزوده/حذف می‌شوند (`SUPPORT_IDS`).
- پشتیبان تنها به تیکت‌ها و تایید/رد پرداخت‌ها دسترسی دارد.

## تیکت‌ها
- لیست تیکت شامل UID کاربر است: `#T123 | UID:...`
- جزئیات تیکت UID را نمایش می‌دهد و دکمه «📋 کپی آیدی» دارد.

## گزارش‌ها
- مسیر: ادمین → «📈 گزارش‌ها»
- متریک‌ها و تعریف آن‌ها در [docs/REPORTS.md](REPORTS.md).

## پرداخت‌ها
- رسیدها به گروه پشتیبانی یا تیکت ارسال می‌شود؛ ادمین/پشتیبان می‌تواند تایید یا رد کند.
- پیام گروه پس از تایید/رد با نام تاییدکننده/ردکننده به‌روزرسانی می‌شود.
